# macOS Safari 构建版验收记录

- 执行时间：2026-09-23（Asia/Shanghai）
- 浏览器：Safari 27.0
- 操作系统：macOS 26.7 (25G227)
- 站点：<https://jjttkid-hw.github.io/lumina-sheets/examples/report.html?accept=1>
- 受测对象：GitHub Pages 在提交 `ee19a90` 后部署的构建产物

## 已执行检查

| 检查 | 结果 | 观察 |
| --- | --- | --- |
| 首屏与 Canvas/无障碍网格初始化 | 通过 | 页面状态为“已生成行式明细 · 可直接编辑或导出”，AX 树出现 `grid`、`row`、`gridcell` |
| 公式示例切换 | 通过 | 状态为“已生成公式示例 · 可直接编辑或导出” |
| 多工作表示例 | 通过 | 状态为“已生成多工作表 · 可直接编辑或导出”，显示 2 张工作表 |
| 百万行分页数据 | 通过 | 状态为“已生成百万行分页数据 · 当前表格只读”，显示 1,000,000 行 × 12 列 |
| 分页生成完整报表 | 通过 | 独立读取 200 行 × 4 列，状态为“已生成分页生成完整报表 · 可直接编辑或导出” |
| 公式栏写入 | 通过 | 在本地同版本构建预览中应用 `=SUM(1,2,3)`，结果显示 6 |
| 空闲绘制 | 通过 | 本地同版本构建预览显示“1 秒内额外绘制 0 次 · 空闲时停止绘制” |

## 证据边界

这份记录证明了真实 Safari 对部署构建产物的页面初始化、报表切换、分页模式和可访问网格入口可用。它没有覆盖原生中文 IME、实体触控、屏幕阅读器、跨设备性能、完整文件往返语料或商业许可证法律复核；这些项目仍不能标记为稳定版已通过。
