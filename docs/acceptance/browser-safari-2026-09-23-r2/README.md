# macOS Safari 线上构建复验记录（r2）

- 执行日期：2026-09-23（Asia/Shanghai）
- 浏览器：macOS Safari 当前运行实例；AX 树未暴露版本号
- 站点：<https://jjttkid-hw.github.io/lumina-sheets/examples/report.html>
- 受测提交：`8445b0a2dde1cce44c228cbbae023605b35c1dda`
- 站点摘要：`791a5888cb5672e3a597062cfc4441a689db8597e26d6dfd32ecf3f1f6a07268`

## 已执行检查

| 检查 | 结果 | 观察 |
| --- | --- | --- |
| 页面初始化 | 通过 | Safari AX 树出现报表入口、Canvas 网格对应的 `table`、`row`、`cell` 语义；状态为“已生成行式明细 · 可直接编辑或导出” |
| 公式示例切换 | 通过 | 点击“公式示例”后状态为“已生成公式示例 · 可直接编辑或导出”，显示 21 行 × 6 列 |
| 公式栏编辑 | 通过 | 在“单元格内容或公式”写入 `=SUM(1,2,3)` 并应用，公式栏保留该公式，结果显示 `6`，状态为“单元格已更新 · 相关公式随之重算” |
| 撤销 | 通过 | 点击“撤销”后当前单元格恢复为原始文本“场景”，结果同步恢复 |
| 绘制统计 | 通过 | 页面显示公式计算次数、缓存项和最近绘制耗时；未观察到错误提示 |

## 证据边界

本记录只证明当前 macOS Safari 页面上的初始化、公式编辑、撤销和状态反馈。它不替代原生中文输入法候选窗口、VoiceOver 实际朗读、实体触控、跨设备性能、完整 Excel/WPS 语料或商业许可证法律复核；这些仍是 v1.0 门槛。
| 刷新恢复 | 通过 | 将 A1 改为 `=SUM(4,5,6)` 后结果为 15；Safari 刷新后公式栏仍为该公式，结果仍为 15，页面保持公式示例状态 |
