# WPS 实际桌面往返检查

环境：macOS 26.7 arm64；WPS Office 2026 秋季更新 12.1.26055（“关于 WPS”实际读取）。时间：2026-09-23。执行者：Codex，经原生桌面 UI 操作。仅打开项目自行生成的合成工作簿，未修改用户其他文件或云端文档。

步骤与观测：

1. 用上一候选 SDK 导出两表合成 XLSX，在 WPS 打开。未出现修复文件提示；富文本、金额 120/30/90、日期、合并标题、隐藏第 7 行及 F 列、冻结首行可见。
2. 定位“业务 数据”A2，输入 200。切换“汇总”，公式栏为 `='业务 数据'!C2*2`，画面结果 ¥340.00；截图见 summary-recalculated.png。
3. 在本地保存，SDK 读取该 WPS 原件。发现 B5 的 `_x005f_x0041_` 被底层读取器误解码，造成文本损坏；该候选因此不能记为全部通过。
4. 修复为从原始 XML 单次读取普通字符串，覆盖共享文本、超链接标签和普通 inlineStr。公开夹具清理作者名与 ICV 后保留全部工作表/字符串 XML；来源与原件/清理版哈希见 ../../../tests/fixtures/wps/README.md。
5. 新候选安装包 b6d6705b28b726aab6700f3e0c7be882cdc247e6f851fff93029701a36bc1266 实际回读此保存文件及再次 XLSX 往返，文字、CRLF、中文/Emoji、公式值 170/340、日期/布尔、富文本、验证、合并、隐藏、冻结和打印语义均通过。结果见 ../browser-candidate-2026-09-23-r11/wps-corpus.json。每次 CI 都重新运行当前包检查。

本轮只完成一个明确合成语料的 WPS 编辑保存闭环；打印预览、数据校验弹窗、更多业务文件及 Microsoft Excel 桌面尚未验证。WPS 补齐了默认字体/纸张元数据，不宣称像素保真或 XML 字节不变。原始版本截图含本机用户信息，仅留本地 artifacts，不纳入公开证据。
