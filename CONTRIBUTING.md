# 参与贡献

欢迎通过 Issue 和 Pull Request 改进 Lumina Sheets。提交前请先阅读 [README.md](README.md) 和相关 [SDK 文档](docs/SDK.md)，确认当前功能与兼容边界。

## 本地开发

使用 Node.js 24 和 npm，在项目目录执行：

```sh
npm ci
npm run dev
```

SDK 示例位于 `http://127.0.0.1:5173/examples/report.html`，性能实验室位于 `http://127.0.0.1:5173/performance`。

提交前运行项目现有检查：

```sh
npm test
npm run format:check
npm run build:all
```

`npm run format` 可格式化现有脚本覆盖的源码与测试文件。`build:all` 构建主应用和 SDK，产物位于 `dist/`。CI 配置会运行上述测试、格式检查和构建；这不代表某次提交已经通过检查，请查看对应运行结果。

## 报告问题

请说明复现步骤、预期与实际行为、项目版本或提交、浏览器与操作系统。提供尽可能小的合成工作簿或代码示例；公式、导入导出问题请列出相关单元格、公式和文件格式。性能问题请提供数据规模、设备、操作步骤与原始测量结果，区分首次加载、计算和绘制耗时。

不要上传真实业务数据、个人信息、访问令牌、密钥或未获授权的文件。截图和日志也应脱敏；优先使用合成测试数据。

## 提交 Pull Request

- 一次提交聚焦一个问题，说明改变的行为、兼容影响与验证结果。
- 为修复和功能增加能证明行为的测试；涉及 Canvas 交互时附复现操作和必要的界面验证。
- 公共 API 或文件兼容范围变化时同步更新文档；不要把有限支持描述为完整 Excel 或 SpreadJS 兼容。
- 保留依赖锁文件；引入依赖或外部代码时说明来源与许可证，不提交 `node_modules`、构建产物或敏感数据。
- 提交你有权贡献的内容。项目代码采用 [Apache License 2.0](LICENSE)，贡献应与此许可证兼容。
